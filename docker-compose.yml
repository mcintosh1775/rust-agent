services:
  postgres:
    image: postgres:16
    environment:
      POSTGRES_PASSWORD: postgres
      POSTGRES_USER: postgres
      POSTGRES_DB: agentdb
    ports:
      - "5432:5432"
    volumes:
      - agentdb-data:/var/lib/postgresql/data
volumes:
  agentdb-data:
